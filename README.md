# MoltenPixelStudio Scoop bucket

Usage:

```powershell
scoop bucket add mps https://github.com/MoltenPixelStudio/scoop-bucket
scoop install linkdrive
```

## Apps

- **linkdrive** — cross-device file explorer with VPS/SFTP + LAN peer access
